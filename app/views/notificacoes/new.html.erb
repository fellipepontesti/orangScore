<% content_for :title, "Enviar notificação" %>

<div class="w-full max-w-3xl mx-auto px-4 py-8">
  <div class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 border-b border-base-200 pb-6 mb-8">
    <div class="flex flex-col gap-1 min-w-0">
      <div class="flex items-center gap-2 text-sm text-orange-500 font-mono font-bold uppercase tracking-wider">
        <%= link_to "Notificações", notificacoes_path, class: "hover:underline" %>
        <span>/</span>
        <span>Enviar</span>
      </div>
      <h1 class="text-3xl sm:text-4xl font-black tracking-tight bg-gradient-to-r from-amber-400 to-orange-500 bg-clip-text text-transparent pb-1">
        Enviar Notificação
      </h1>
      <p class="text-base-content/60 text-sm sm:text-base">Crie uma mensagem única para aparecer na área de notificações de todos os usuários.</p>
    </div>

    <%= link_to notificacoes_path, class: "btn btn-ghost btn-sm sm:btn-md gap-2 rounded-2xl shrink-0" do %>
      <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="2" stroke="currentColor" class="w-4 h-4">
        <path stroke-linecap="round" stroke-linejoin="round" d="M9 15 3 9m0 0 6-6M3 9h12a6 6 0 0 1 0 12h-3" />
      </svg>
      Voltar
    <% end %>
  </div>

  <div class="bg-base-100 border border-base-300 rounded-box shadow-lg p-8">
    <%= render "form", notificacao: @notificacao %>
  </div>
</div>
