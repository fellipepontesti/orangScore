<% content_for :title, "Editar palpite" %>

<div class="flex-1 flex flex-col w-full">
  <div class="flex items-center justify-between p-8">
    <h1 class="font-bold text-4xl">Editar palpite</h1>
  </div>

  <div class="w-full px-4 pb-8">
    <%= render "form", palpite: @palpite %>
  </div>
</div>